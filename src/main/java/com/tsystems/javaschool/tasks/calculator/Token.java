package com.tsystems.javaschool.tasks.calculator;/** * Class Token: contains information of all token in the statement */class Token {    private boolean type;   // 0 - operator, 1 - value    private char op;        // stores char of the operator    private double value;   // stores double value of the operand    private int priority;   // priority of the operator    private String token;   // original string token    Token(String token) {        String operators = "+-*/()";        this.token = token;        if (isNumeric(token)) {            this.value = Double.parseDouble(token);            this.type = true;        } else if (operators.contains(String.valueOf(token.charAt(0)))) {            this.op = token.charAt(0);            this.type = false;            if (token.charAt(0) == '(')                this.priority = 0;            else if (token.charAt(0) == ')')                this.priority = 1;            else if (token.charAt(0) == '+' || token.charAt(0) == '-')                this.priority = 2;            else if (token.charAt(0) == '*' || token.charAt(0) == '/')                this.priority = 3;        }    }    private static boolean isNumeric(String str)    {        return str.matches("-?\\d+(\\.\\d+)?");    }    boolean isType() {        return type;    }    char getOp() {        return op;    }    double getValue() {        return value;    }    int getPriority() {        return priority;    }    String getToken() {        return token;    }}